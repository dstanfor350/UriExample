		UriParse	

UiParse is a exercise in separating the baseurl from the endpoint and splitting
that endpoint into a list.  This could be used in a manual routing to a 
Web API endpoint.  But really, it is just an exercise.

NUnit tests are included.